// Compatibility entry point for existing imports. The implementation remains
// centralized in pages/imageUtils.ts to preserve the current module behavior.
export { formatImageUrl, normalizeBoundingBox } from "../pages/imageUtils";