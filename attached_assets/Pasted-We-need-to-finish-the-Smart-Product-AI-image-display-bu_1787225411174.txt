We need to finish the Smart Product AI image-display bug now. Work on this as ONE controlled debugging and repair task.

IMPORTANT: Preserve the existing system. Do NOT rewrite the application or redesign unrelated features.

The system has:
- Vendor dashboard
- Buyer dashboard
- Admin dashboard
- AI image analysis
- Product detection
- Product creation/update
- Orders
- Checkout
- Authentication/roles

These existing flows must remain intact.

==================================================
CURRENT KNOWN INFORMATION
==================================================

The original problem is:

Vendor uploads an image
→ AI analyzes it
→ detected products are saved
→ buyer dashboard displays products
→ buyer should see the REAL uploaded image belonging to each product.

Previously, the image sometimes displayed a static Unsplash image or the wrong/broken image.

We have already discovered the following:

1. Uploaded files physically exist in:

uploads/

For example:

uploads/f1b2c3d4.jpg

2. FastAPI serves uploads using:

app.mount("/uploads", StaticFiles(...), name="uploads")

3. Product.image_url is now being populated.

4. Windows path separators were previously causing values such as:

uploads\f1b2c3d4.jpg

instead of:

uploads/f1b2c3d4.jpg

5. The frontend previously had fragile image URL handling that could produce:

http://localhost:8000/uploads/uploads\f1b2c3d4.jpg

6. The frontend also contains Unsplash fallback images. These MUST NOT be used when a valid uploaded product image exists.

7. The current frontend build is blocked by:

smart_product_ai_frontend/src/pages/Review.tsx

The previous inspection confirmed that Review.tsx contains a valid component ending around line 195, followed by an orphaned duplicated JSX fragment beginning around line 196.

==================================================
PRIMARY OBJECTIVE
==================================================

Fix the complete REAL uploaded-image flow so that:

Vendor uploads Product A image
→ Product A gets the correct image URL
→ AI analysis keeps that image association
→ Confirm & Save keeps that image association
→ Product database contains the correct image URL
→ GET /api/v1/products returns the correct image_url
→ Buyer dashboard constructs the correct URL
→ Browser successfully loads the actual uploaded file
→ Product A displays Product A's image.

If multiple products come from the same uploaded image, they may legitimately share the source image, but different uploaded images MUST NEVER become the same static fallback image.

==================================================
STEP 1 — FIX THE CURRENT BUILD BLOCKER
==================================================

Inspect:

smart_product_ai_frontend/src/pages/Review.tsx

The previous investigation found that a valid component ends around line 195 and an orphaned duplicated JSX fragment follows it.

Remove ONLY the orphaned/duplicated JSX fragment.

Do not rewrite the component.
Do not change its business logic.
Do not change its API calls.
Do not change its UI behavior.

Then run:

cd smart_product_ai_frontend
npm run build

Fix only errors directly caused by this malformed Review.tsx structure.

Do not suppress errors with:
- @ts-ignore
- @ts-nocheck
- any
- disabling TypeScript checks.

==================================================
STEP 2 — VERIFY THE BACKEND IMAGE STORAGE
==================================================

Inspect these files:

app/modules/media/local_storage.py
app/modules/media/service.py
app/modules/media/models.py
app/modules/intelligence/service.py
app/modules/catalog/models.py
app/modules/products/service.py
app/modules/products/router.py
app/main.py

Trace the image_url from upload to Product.

The canonical database value MUST use URL-style forward slashes:

uploads/<filename>

NOT:

uploads\<filename>

Do not use Windows filesystem paths as browser URLs.

Physical filesystem paths may use Windows separators internally, but values exposed to the frontend must use forward slashes.

Make sure Product.image_url is populated when products are created or updated through batch-update.

Do not duplicate "uploads" in the URL.

For example:

CORRECT database value:
uploads/f1b2c3d4.jpg

CORRECT browser URL:
http://localhost:8000/uploads/f1b2c3d4.jpg

INCORRECT:
http://localhost:8000/uploads/uploads/f1b2c3d4.jpg

INCORRECT:
http://localhost:8000/uploads/uploads\f1b2c3d4.jpg

==================================================
STEP 3 — VERIFY FASTAPI STATIC FILE SERVING
==================================================

In app/main.py, make the uploads directory resolution deterministic.

Use the project root rather than relying on ambiguous relative paths.

For example, use pathlib:

from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent.parent
UPLOADS_DIR = BASE_DIR / "uploads"

Ensure the directory exists.

Then mount:

app.mount(
    "/uploads",
    StaticFiles(directory=str(UPLOADS_DIR)),
    name="uploads"
)

Do NOT create a second unrelated uploads directory inside app/.

The upload service and StaticFiles MUST point to the same physical uploads directory.

==================================================
STEP 4 — VERIFY PRODUCT IMAGE PERSISTENCE
==================================================

Inspect:

app/modules/products/service.py

Find batch_update_products.

When an existing Product is updated, ensure image_url from the incoming product data is preserved when valid.

When a new Product is created, ensure image_url is passed into Product.

Normalize URL separators before persistence:

backslash → forward slash

But do NOT convert arbitrary filesystem paths incorrectly.

The resulting Product.image_url should be something like:

uploads/f1b2c3d4.jpg

==================================================
STEP 5 — VERIFY FRONTEND IMAGE URL HANDLING
==================================================

Inspect:

smart_product_ai_frontend/src/pages/dashboard.tsx

and:

smart_product_ai_frontend/src/features/analysis/ProductCard.tsx

and any centralized image utility such as:

smart_product_ai_frontend/src/pages/imageUtils.ts

Prefer ONE reliable image URL normalization function instead of several conflicting implementations.

The function must correctly handle:

1. null
2. undefined
3. empty string
4. uploads/file.jpg
5. uploads\file.jpg
6. /uploads/file.jpg
7. http://localhost:8000/uploads/file.jpg
8. an already-valid absolute URL
9. blob URLs if they are intentionally used for local preview

It must never produce:

/uploads/uploads/file.jpg

It must never leave a Windows backslash in an HTTP URL.

For a database value:

uploads/f1b2c3d4.jpg

the result must be:

http://localhost:8000/uploads/f1b2c3d4.jpg

Use the existing environment/API configuration where appropriate rather than hardcoding localhost if the project already has a backend-origin configuration.

==================================================
STEP 6 — REMOVE THE STATIC IMAGE PROBLEM
==================================================

Inspect all product image rendering locations.

Search the project for:

unsplash.com
formatImageUrl
image_url
imageUrl
onError

Do NOT blindly remove every fallback.

Instead:

- A fallback may exist for a genuinely missing image.
- But a valid uploaded image MUST always be attempted first.
- A failed image must not cause every product to display the same misleading Unsplash product image.

Prefer a neutral "image unavailable" fallback if the project already has one.

Do not replace real product images with a generic static image.

==================================================
STEP 7 — VERIFY IMAGE/PRODUCT ASSOCIATION
==================================================

Inspect:

smart_product_ai_frontend/src/features/analysis/AnalysisViewer.tsx

Verify that when Confirm & Save is performed, the correct image_url is associated with the detected products.

Do not change the AI detection logic unless the investigation proves it is necessary.

The important rule is:

Product A must retain Product A's source image.

Do not use array index matching to associate unrelated images.

Do not introduce static images.

==================================================
STEP 8 — BUILD AND TEST
==================================================

After making the minimal required corrections:

Backend:

python -c "from app.main import app; print('backend import ok')"

Frontend:

cd smart_product_ai_frontend
npm run build

Then verify the upload directory exists and contains actual image files.

If possible, start the backend and directly test an actual image:

GET /uploads/<actual-file-name>

The expected result is HTTP 200, not 404.

Then test:

GET /api/v1/products

Verify that products contain non-null image_url values where an image exists.

==================================================
CRITICAL SAFETY RULES
==================================================

DO NOT modify unrelated functionality.

DO NOT:
- rewrite authentication
- rewrite database models
- rewrite AI processing
- rewrite orders
- rewrite checkout
- rewrite admin
- rewrite vendor dashboard
- rewrite buyer dashboard design
- change routing unnecessarily
- change migrations unnecessarily
- upgrade dependencies unnecessarily
- replace the frontend framework
- replace FastAPI
- change the database
- delete existing functionality
- generate fake images
- use Unsplash as the actual product image
- create a second uploads directory

Only change files that are genuinely required for:
1. the Review.tsx build blocker
2. image storage/path correctness
3. static image serving
4. product image persistence
5. frontend image URL rendering

==================================================
FINAL REPORT
==================================================

When finished, give me a concise report containing:

1. ROOT CAUSE
2. FILES CHANGED
3. EXACT FIX MADE IN EACH FILE
4. FRONTEND BUILD RESULT
5. BACKEND IMPORT RESULT
6. STATIC IMAGE URL TEST RESULT
7. PRODUCTS API IMAGE_URL RESULT
8. WHETHER THE REAL UPLOADED IMAGE CAN NOW REACH THE BUYER DASHBOARD
9. ANY REMAINING BLOCKER

Do not make additional changes after the tests pass.

If a test fails because of an unrelated issue, STOP and report it instead of modifying unrelated parts of the application.